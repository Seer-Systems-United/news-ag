[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { workspace = true, features = ["fullstack"] }
news-ag = { workspace = true, features = ["async", "serde"] }

[features]
server = ["dioxus/server"]
